services:
  bracketfrontend:
    image: bracketfrontend
    build:
      context: bracket-frontend
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: development
    ports:
      - 3000:3000
